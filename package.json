{
  "name": "snakebyte",
  "private": true,
  "packageManager": "npm@11.4.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "turbo run dev --filter=web",
    "dev:api": "turbo run dev --filter=api",
    "build": "turbo run build",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "dependencies": {
    "@snakebyte/shared": "^0.1.0",
    "typeorm": "^0.3.25"
  },
  "files": [
  "dist",
  "certs"
]
}
